<div class="">
    <div class="page-title">
        <div class="title_left">
            <h3>Admin Users
                <small> / List of all administrators.</small>
            </h3>
        </div>
        <div class="title_right">
            <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right top_search">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Search for...">
                    <span class="input-group-btn">
                      <button class="btn btn-default" type="button">Go!</button>
                    </span>
                </div>
            </div>
        </div>
    </div>
    <div class="clearfix"></div>
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <a href="<?= $this->url('admin.users.action', ['action' => 'edit', 'id' => 0]) ?>"
                       class="btn btn-default pull-right btn-success"> Add new User
                    </a>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <?php if($list->getTotalItemCount() > 0): ?>
                        <table class="table">
                            <thead>
                            <tr>
                                <th>Active</th>
                                <th>Full Name</th>
                                <th>Email</th>
                                <th>Last Login</th>
                                <th>Created</th>
                                <th></th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php foreach($list as $one): ?>
                                <tr>
                                    <th>
                                        <span class="label label-<?= $one->status ? 'success' : 'danger' ?>"><?= $one->status ? 'Yes' : 'No' ?></span>
                                    </th>
                                    <td><?= $one->first_name ?> <?= $one->last_name ?></td>
                                    <td><?= $one->email; ?></td>
                                    <td><?= $one->last_login ? date('H:i d.m.Y', strtotime($one->last_login)) : '-' ?></td>
                                    <td><?= date('d.m.Y', strtotime($one->created_at)) ?></td>
                                    <td>
                                        <span class="pull-right">
                                            <a href="<?= $this->url('admin.users.action', ['action' => 'edit', 'id' => $one->admin_user_id]) ?>">
                                                <i class="glyphicon glyphicon-pencil"></i>
                                            </a> &nbsp;
                                        </span>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                        <?= $this->paginationControl($list, 'Sliding', 'admin/pagination', ['route' => 'admin.users']) ?>
                    <?php else: ?>
                        <div class="col-md-offset-3 col-sm-offset-3 col-md-6 col-sm-6 col-xs-12">
                            <div class="jumbotron text-center">
                                <h3>There are added Admin Users!</h3>
                            </div>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>