language: php
matrix:
  fast_finish: true
  include:
    - php: 7
install: composer --working-dir=application install --no-interaction
script:
  - composer --working-dir=application test
  - composer --working-dir=application cs-check
after_script:
  - composer --working-dir=application upload-coverage
